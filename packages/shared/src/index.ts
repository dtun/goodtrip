// @goodtrip/shared — shared types and constants.
// Data-model types (spec section 3) and AI action contracts (spec section 8)
// land here. Placeholder export keeps the module valid until then.
export {};
