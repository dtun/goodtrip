# @goodtrip/shared

Shared TypeScript types and constants used across the mobile app, web app, and
Supabase edge functions — primarily the data-model types (spec section 3) and
the AI action contracts (spec section 8).

> **Status:** placeholder. Populated as the data model and AI action schema
> land (see the repo's GitHub issues / milestones).
